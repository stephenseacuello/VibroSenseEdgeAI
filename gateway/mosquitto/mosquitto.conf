# VibroSense Edge AI — dev Mosquitto config.
# Local-only (no auth) is fine for the lab. Tighten for any non-bench deployment.

listener 1883
allow_anonymous true

persistence true
persistence_location /var/lib/mosquitto/

log_dest stdout
log_type warning
log_type error
